<%= form_for ([@trip, @trip.transactions.build]) do |f| %>
   
    
    <%= f.collection_select :dcategory_id, Dcategory.all, :id, :name, {promt: "Chose your Daily expenses"} %>
    <%= f.label :Detail %>
    <%= f.text_field :item %>
    
    <%= f.label :amount %>
    <%= f.text_field :amount %>
    <%= f.submit %>

<% end %>