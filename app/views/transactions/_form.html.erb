<%= form_for ([@trip, @trip.transactions.build]) do |f| %>

    

    <%= f.label :Detail %>
    <%= f.text_field :item %>
    
    <%= f.label :amount %>
    <%= f.text_field :amount %>
    <%= f.submit %>

<% end %>