<br>
<center><h1>Trips</h1></center>



  <%= link_to '<button type="button">Create new Trip</button>'.html_safe, new_trip_path %> 

<table class = "table table-bordered table-striped">
   <thead>.
      <tr>
         <th>Place</th>
         <th>Purpose</th>
         <th> </th>
      </tr>
   </thead>
   
   <tbody>
      <% @trips.each do |trip| %>
         <%if trip.user_id == current_user.id %>
         <tr>
            <td><%= trip.place %></td>
            <td><%= trip.purpose %></td>
            <td><%= link_to '<button type="button">Show</button>'.html_safe, trip_path(trip) %></td>
         </tr>
         <% end %>
      <% end %>
   </tbody>
   
</table>
<%= link_to '<button type="button">Add new Trip</button>'.html_safe, new_trip_path %> 

  